# blive-monitor 运行时依赖
#
# 直播状态检测（check_status.py）仅使用 Python 标准库，无需额外依赖。
# 抖音/快手新作品检测（check_new_posts.py / tools/kuaishou_probe.py）需要无头 Chromium。
#
# 浏览器启动统一走 backend.adapters._browser：优先 patchright（浏览器级隐身抗快手风控，
# RSSHub 同款方案），未安装时回退原生 playwright（隐身减弱但不中断监控）。
# patchright 是 Playwright 的反检测 fork（独立包，不依赖 playwright 包），自带打补丁的
# Chromium，需 `patchright install --with-deps chromium` 下载。
# Python 版本要求: 3.8+
playwright==1.58.0        # 兜底：patchright 不可用时退回原生 playwright
patchright==1.61.2        # 优先：浏览器级隐身抗风控

# 校验工作流 YAML（test_refactor_edge 等测试需要），CI 安装避免 ImportError
pyyaml>=6.0

# ===== 阶段四后端依赖 =====
# Web 框架（sync def 路由 + 线程池）；与 sync SQLAlchemy 零冲突。
fastapi==0.128.2
# ASGI 服务器（含标准依赖：httptools / uvloop / watchgod 等）。
uvicorn==0.32.0
# ORM / DB 访问层（sync 2.0），SQLite 方言零额外依赖。
sqlalchemy==2.0.51
# 后端自驱 scheduler（AsyncIOScheduler）。
apscheduler==3.11.3
# 推送 / 检测逻辑复用的 HTTP 客户端（common / push_utils 等）。
httpx==0.27.2
# 备选表单 / 文件导入端点（低耦合，预留）。
python-multipart==0.0.12
# 请求/响应模型（FastAPI 自带，此处显式声明以便锁定版本）。
pydantic==2.9.2
